### Objective

Finalize the color palette, typography, and global UI spacing to ensure 100% pixel-perfect fidelity with the mockup.

### Tasks

* Refine CSS variables in `index.css` for background, text, and border colors.
* Adjust padding and spacing in the main `DashboardLayout`.
* Ensure scrollbars are custom styled to match the dark theme.
* Check font weights and letter spacing across all headers.

### Acceptance criteria

* The entire application is visually cohesive and identical to the original design reference.
* No default browser styles (like scrollbars) disrupt the immersion.
