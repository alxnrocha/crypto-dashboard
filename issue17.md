### Objective

Redesign the TopBar component to exactly match the elements and styling of the mockup.

### Tasks

* Update the Search input: adjust width, align to right, add the `⌘K` keyboard shortcut badge, and use the correct placeholder text.
* Add the Currency Selector dropdown ("USD v").
* Add the Moon (Dark mode) toggle icon.
* Update the Notifications (Bell) icon and add an unread indicator dot.
* Add a placeholder Profile Avatar on the far right.

### Acceptance criteria

* TopBar perfectly matches the visual structure of the mockup.
* Spacing between icon buttons is precise.
* Input background, borders, and text colors are matched.
