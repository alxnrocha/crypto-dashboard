export const mockSparklineRed = Array.from({ length: 15 }).map(() => ({
  value: Math.random() * 100,
}));
export const mockSparklineGreen = Array.from({ length: 15 }).map(() => ({
  value: Math.random() * 100 + 50,
}));
